# GRIFF Frame — Phase 4 Dev Log
**날짜**: 2026-03-03 23:30
**세션**: Phase 4 (Auth + 프로젝트 관리 + 배포 준비)

---

## 완료된 작업

### Phase 1~3 (이전 세션)
- 프로토타입 → 파일 분리 → Supabase 연동 → 코멘트 CRUD → 내보내기 4종 → 게스트 접근
- Git 커밋: Phase 2 (7bb9e33), Phase 3 (1922892)
- GitHub: https://github.com/sunghyeonhwang/GRIFF-F

### Phase 4 구현 완료 (코드 작성)
1. **`public/js/auth.js`** — Supabase Auth 모듈
   - signUp, signIn, signInWithMagicLink, signOut, getUser, onAuthStateChange
   - fetchProjects, createProject, deleteProject, fetchProjectByShareToken
   - `window.__griffAuth`로 노출

2. **`public/js/app.js`** — 전면 리팩터링 (~900줄)
   - `LoginPage` — 이메일/비밀번호 로그인 + 회원가입
   - `ProjectListPage` — 프로젝트 목록, 생성, 삭제
   - `CreateProjectModal` — 제목 + Vimeo URL 입력
   - `ReviewPage` — 기존 App 리네임, props로 project 데이터 수신
   - `Root App` — 상태 기반 라우팅 (loading → login → projects → review)
   - `Spinner`, `ErrorBanner` 컴포넌트
   - VideoPlayer가 vimeoId를 동적으로 받도록 변경

3. **`vercel.json`** — Vercel 배포 설정
   ```json
   { "buildCommand": "", "outputDirectory": "public", "rewrites": [{"source":"/(.*)", "destination":"/index.html"}] }
   ```

4. **`supabase/rls-policies.sql`** — 업데이트
   - `members_delete_own_projects` 정책 추가
   - `members_update_comments` 정책 수정 (프로젝트 소유자도 코멘트 수정 가능)

5. **`public/index.html`** — auth.js 스크립트 태그 추가

---

## 현재 블로커: Supabase Auth 회원가입 에러

### 증상
- 회원가입 시 **"Database error saving new user"** 에러 (HTTP 500)
- curl로 직접 API 호출해도 동일 에러 발생 → 클라이언트 코드 문제 아님
- Supabase 서버 측 문제 확인됨

### curl 테스트 결과
```bash
curl -X POST 'https://aoohbaceqfjangcomlmh.supabase.co/auth/v1/signup' \
  -H 'apikey: ...' \
  -H 'Content-Type: application/json' \
  -d '{"email":"test2@griff.co.kr","password":"test1234"}'

# 응답:
# {"code":500,"error_code":"unexpected_failure","msg":"Database error saving new user"}
```

### 이미 시도한 것
- Supabase Auth → Email → "Confirm email" 비활성화 → 효과 없음

### 다음 확인사항
Supabase Dashboard에서 확인 필요:
1. **Logs → Auth Logs** 에서 상세 에러 메시지 확인
2. **SQL Editor**에서 아래 쿼리 실행:
   ```sql
   -- auth 스키마 상태 확인
   SELECT count(*) FROM auth.users;

   -- auth.users 커스텀 트리거 확인
   SELECT trigger_name, event_manipulation, action_statement
   FROM information_schema.triggers
   WHERE event_object_schema = 'auth' AND event_object_table = 'users';
   ```
3. 트리거가 있으면 제거 후 재시도
4. 문제 지속 시 Supabase 프로젝트 재생성 고려

---

## 미실행 RLS 정책 (Supabase Dashboard에서 실행 필요)

```sql
-- 프로젝트 삭제 정책 (신규)
CREATE POLICY "members_delete_own_projects"
  ON projects FOR DELETE
  TO authenticated
  USING (created_by = auth.uid());

-- 코멘트 업데이트 정책 수정 (기존 정책 DROP 후 재생성)
DROP POLICY IF EXISTS "members_update_own_comments" ON comments;
CREATE POLICY "members_update_comments"
  ON comments FOR UPDATE
  TO authenticated
  USING (
    author_id = auth.uid()
    OR project_id IN (SELECT id FROM projects WHERE created_by = auth.uid())
  );
```

---

## Phase 4 남은 작업

1. **Auth 에러 해결** — Supabase Auth 500 에러 수정
2. **회원가입/로그인 테스트** — 전체 인증 흐름 검증
3. **프로젝트 CRUD 테스트** — 생성, 목록, 삭제
4. **RLS 정책 실행** — 위 SQL을 Supabase Dashboard에서 실행
5. **Vercel 배포** — 환경변수 설정 + 배포
6. **전체 시나리오 테스트** — 멤버 로그인 → 프로젝트 생성 → 코멘트 → 공유 링크 → 게스트 코멘트 → 내보내기
7. **DEV_LOG + Git/Push**

---

## Supabase 정보
- **URL**: https://aoohbaceqfjangcomlmh.supabase.co
- **기존 프로젝트 ID**: 7a0fcef1-1951-4ad7-9695-b8567aeb1ba9
- **Share Token**: 5b9316a1-db64-41cc-939e-b17203a37b68

## 파일 구조
```
GRIFF_Frame/
├── public/
│   ├── index.html          ← CDN + 스크립트 로드
│   ├── css/style.css       ← 커스텀 스타일
│   └── js/
│       ├── supabase-client.js  ← Supabase 초기화 + Comments API
│       ├── auth.js             ← Auth + Projects API
│       ├── export.js           ← 4종 내보내기
│       └── app.js              ← React 앱 (전체 UI)
├── supabase/
│   ├── schema.sql          ← DB 스키마
│   └── rls-policies.sql    ← RLS 정책
├── scripts/serve.js        ← Express 개발 서버
├── vercel.json             ← Vercel 배포 설정
├── .env                    ← 환경변수 (gitignored)
└── prototype-v1.html       ← 초기 프로토타입
```
