# DEV_LOG — 2026.03.03 22:15

## 세션 요약

Phase 2 파일 분리 완료 → Phase 2.5 Supabase 연동 → Phase 3 핵심 기능 구현.

---

## 진행 내역

### 1. Phase 2 완료: 멀티파일 구조 전환

- prototype-v1.html → public/ 멀티파일 분리 완료
  - `public/index.html` — HTML 셸 (CDN 로드 + Tailwind 설정)
  - `public/css/style.css` — 스타일시트
  - `public/js/app.js` — React 컴포넌트 전체
  - `scripts/serve.js` — Express 개발 서버
- `npm start` → `http://localhost:3000` 정상 동작 확인
- 모든 리소스 200 OK, Vimeo Player 정상 로드

### 2. Phase 2.5 완료: Supabase 연동

- Supabase 프로젝트 연결 (`aoohbaceqfjangcomlmh.supabase.co`)
- `.env` 파일 생성 (SUPABASE_URL, SUPABASE_ANON_KEY)
- `supabase/schema.sql` 실행 — projects, comments 테이블 생성
- `supabase/rls-policies.sql` 실행 — RLS 정책 적용
- Realtime 활성화 (`ALTER PUBLICATION supabase_realtime ADD TABLE comments`)
- 연동 검증 완료:
  - projects SELECT (anon) ✅
  - comments INSERT (anon/게스트) ✅
  - comments SELECT (anon) ✅
  - RLS — anon projects INSERT 차단 ✅
- `public/js/supabase-client.js` — Supabase 클라이언트 + Comments API (CRUD + Realtime 구독)
- `public/js/app.js` — 로컬 배열 → Supabase 전환
  - DB에서 코멘트 로드
  - 코멘트 추가 → DB INSERT
  - Resolve 토글 → DB UPDATE (optimistic UI)
  - Realtime 구독 (INSERT/UPDATE/DELETE 자동 반영)
  - Supabase 미연결 시 FALLBACK_COMMENTS 폴백

### 3. Phase 3 완료: 핵심 기능

#### 3-1. 배속 제어 (Agent A — player)
- 이미 Phase 1에서 구현 완료 (0.5x, 1x, 1.5x, 2x)
- Vimeo Player SDK `setPlaybackRate()` 정상 동작

#### 3-2. 내보내기 4종 (Agent B — export)
- `public/js/export.js` 신규 생성
- **Premiere Pro XML** (.xml) — XMEML v4 마커 포맷
- **Final Cut Pro** (.fcpxml) — FCPXML v1.10 마커 포맷
- **DaVinci Resolve** (.edl) — EDL 마커 포맷
- **CSV** (.csv) — UTF-8 BOM, 한글 호환
- ExportDropdown에 실제 다운로드 기능 연결
- CSV 다운로드 테스트 성공

#### 3-3. 게스트 접근 (Agent C — comment+guest)
- 공유 링크: `?share={share_token}` URL 파라미터 방식
- GuestNameModal 컴포넌트 — 이름 입력 모달 (backdrop blur)
- localStorage에 게스트 이름 저장 (`griff_guest_name`)
- ShareModal에 실제 share_token 기반 URL 표시
- 게스트 코멘트 → Supabase INSERT (author_id = NULL)

---

## 파일 변경 내역

### 신규 생성
- `public/js/export.js` — 내보내기 모듈 (4종 포맷)

### 수정
- `public/index.html` — Supabase CDN + supabase-client.js + export.js 로드 추가
- `public/js/supabase-client.js` — 전면 재작성 (하드코딩 → 실제 Supabase 연결, Comments API)
- `public/js/app.js`
  - Supabase 초기화 + dbToComment 변환 함수
  - App: DB 코멘트 로드 + Realtime 구독 + 게스트 모드
  - handleResolve: optimistic UI + DB UPDATE
  - handleAddComment: DB INSERT + Realtime 중복 방지
  - ExportDropdown: 실제 다운로드 기능 연결
  - ShareModal: share_token 기반 URL
  - GuestNameModal: 게스트 이름 입력 (신규)

### SQL 실행 (Supabase 대시보드)
- `supabase/schema.sql` — projects + comments 테이블
- `supabase/rls-policies.sql` — 8개 RLS 정책

---

## DB 상태

- **projects**: 1개 (GRIFF 브랜드 필름 v3, share_token: `5b9316a1-db64-41cc-939e-b17203a37b68`)
- **comments**: 2개 (테스트 데이터)
- **Project ID**: `7a0fcef1-1951-4ad7-9695-b8567aeb1ba9`

---

## Git 기록

- `e5baed1` — Phase 1 완료: GRIFF Frame 프로토타입
- `7bb9e33` — Phase 2 완료: 멀티파일 구조 전환 + Supabase 스키마 준비
- (이번 커밋) — Phase 3 완료: Supabase 연동 + 내보내기 + 게스트 접근

---

## 다음 단계 (Phase 4)

- [ ] 내부 팀 멤버 인증 (Supabase Auth — 이메일/비밀번호 또는 Magic Link)
- [ ] 프로젝트 생성/목록 UI (멤버 전용, Vimeo URL 등록)
- [ ] 에러 핸들링 통합 (네트워크 오류, Vimeo 로드 실패 등)
- [ ] 로딩 상태 UI (스피너, 스켈레톤)
- [ ] Vercel 배포 설정 (vercel.json + 환경변수)
- [ ] 최종 테스트 시나리오 실행
